{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "pnpm build",
  "devCommand": "pnpm dev",
  "installCommand": "pnpm install --ignore-scripts=false",
  "framework": "nextjs",
  "outputDirectory": ".next",
  "build": {
    "env": {
      "DATABASE_URL": "file:./dev.db"
    }
  }
}
